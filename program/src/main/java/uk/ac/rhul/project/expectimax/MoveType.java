package uk.ac.rhul.project.expectimax;

/**
 * An enum used tolabel if the move is made by the 'player' or if the move is adding a random tile to the grid.
 */
public enum MoveType
{
    PLAYER_MOVE, GRID_MUTATION;
}
